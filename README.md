# SimpleAudioSpritePacker

It is a very simple tool for packing audiosprite.

## How to use
1. Clone and run `npm install` .
2. Please put your sound files into `input` folder.
3. Run `npm start` , if you use Windows, you can double `run.bat` file to fast pack.
4. Finish.
